<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.driverapp.dao.DriverDao">
    <!--获取司机个人信息-->
    <select id="findDriver" parameterType="com.driverapp.entity.DriverMesInfo" resultType="com.driverapp.entity.DriverMesInfo">
        select
        a.user_nick_name userNickName,
        a.user_head userHead,
        a.user_name userName,
        a.user_phone userPhone
        from t_sys_user a
        where a.user_code = #{userCode}
        and a.is_deleted = 0
        and a.user_role = 2
    </select>



</mapper>
